import { SearchTeamPipe } from './search-team.pipe';

describe('SearchTeamPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTeamPipe();
    expect(pipe).toBeTruthy();
  });
});
