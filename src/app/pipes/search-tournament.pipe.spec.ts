import { SearchTournamentPipe } from './search-tournament.pipe';

describe('SearchTournamentPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTournamentPipe();
    expect(pipe).toBeTruthy();
  });
});
